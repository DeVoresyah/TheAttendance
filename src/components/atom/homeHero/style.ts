import {connect} from '@theme';

export default connect({
  container:
    'flex rounded-b-super-lg bg-product-500 items-center justify-center py-12',
  time: 'text-super-lg font-bold text-black text-center mb-2',
  date: 'text-sm font-bold text-black text-center',
});
