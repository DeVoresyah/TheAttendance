import {connect} from '@theme';

export default connect({
  contentContainer: 'p-5',
  emptyState: 'flex items-center justify-center p-0',
  emptyTitle: 'text-xl font-bold text-black text-center',
});
